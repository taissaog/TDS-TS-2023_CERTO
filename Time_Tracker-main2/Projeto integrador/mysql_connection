const knex = require("knex");

module.exports = knex({
    client: "mysql",
    connection: {
        host: "127.0.0.1",
        port: 3306,
        user: "root",
        password: "1234",
        database: "empresa",
    },
    migrations: {
        directory: './migrations'
    },
    seeds: {
        directory: './seeds'
    }
});